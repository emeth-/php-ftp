Two simple libraries that should 'mirror' each other, one for sftp connections via php and one for ftp connections.

The source code is fairly straightforwad, open up either ftp.php or sftp.php to see the functions available.